# Phase 9.1 — live source-fidelity audit, 2026-09-23

Command: `TotalUpdater.Next.Tests.exe --audit-catalog-version-fidelity` with `COMMANDER_PATH=C:\Program Files\Total Commander`.
The command queried every one of 165 embedded entries, inspected reachable ZIPs in memory, and did not install or save plugin packages. [Per-entry log](2026-09-23-phase9.1-live.log) includes canonical source/authority/version, source response statuses, package URL result, verified binary aliases, raw PE version, and normalized local version.

| Category | Earlier complete run | Last run with degraded network |
| --- | ---: | ---: |
| OK | 1 | 0 |
| NeedsOfficialSource | 88 | 12 |
| PackageIdentityMismatch | 19 | 5 |
| PackageMissing | 42 | 100 |
| SourceLagging | 9 | 8 |
| SourceUnavailable | 1 | 36 |
| VersionDisagreement | 5 | 4 |
| Total | 165 | 165 |

The last run saw widespread source timeouts, so its `SourceUnavailable` and `PackageMissing` counts are transient observations, not durable catalog-quality conclusions. The earlier run (before source-status detail was added to the output) reached substantially more packages. Some `PackageIdentityMismatch` findings involve an additional, unrelated link while another URL for the same entry does contain the expected alias; each URL must be reviewed separately before changing catalog metadata. The audit intentionally does not rewrite the embedded or user catalog.

Verified corrections: Imagine's author markup reports 2.7.0 when reachable; Total7zip's installed PE `0, 8, 5, 6` normalizes to the official public version `8.56`; FileInfo's PE `2, 2, 3, 0` normalizes to `2.23`. Nine installed entries still show `SourceLagging` against currently reachable catalog sources. A mismatched ZIP is blocked before automatic installation; a selected URL remains `Unverified` until package contents are inspected.

Installed coverage on the supplied `Wincmd.ini`: 44/48 logical entries (91.7%) match the embedded catalog. MultiArc, Cloud, IECache and ReadPE remain unmatched without reliable identity evidence.

Remaining source work: independently verify each mismatched URL and locate higher-authority sources for community-only entries. No claim is made that all 165 packages are available, current, or safe to install.
